# 启用API配置模式 - 使用manager-api
read_config_from_api: true

# Manager-API配置
manager-api:
  secret: e57e7eaa-6636-4cf0-8627-a6cdfb00145b
  url: http://xiaozhi-esp32-server-web:8002/xiaozhi

# 服务器基础配置
server:
  ip: 0.0.0.0
  port: 8000
  http_port: 8003

# 本地模块配置
local_override:
  selected_module:
    ASR: SenseVoiceStream
  ASR:
    SenseVoiceStream:
      type: sensevoice_stream
      model_dir: models/SenseVoiceSmall
      output_dir: tmp/
      chunk_size: 1024
      enable_realtime: true
      vad_model: fsmn-vad